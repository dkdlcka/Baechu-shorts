pillow>=10.0
numpy>=1.24
pyyaml>=6.0
edge-tts>=6.1        # 무료 한국어 TTS (Microsoft Edge 음성)
imageio-ffmpeg>=0.5  # 시스템 ffmpeg가 없을 때 쓰는 번들 바이너리
anthropic>=0.40      # (선택) Claude로 대본 자동 생성
pydantic>=2.0
gradio_client>=1.0  # (선택) 무료 Hugging Face Spaces 생성
